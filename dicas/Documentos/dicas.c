TAD.c

typedef (void*) item;

FP{
	(void*)	*v;
	int n;
	fcmp cmp;
}

fila->cmp(fila->v[i],fila->v[j]){
}

typedef int (*fcmp)(void*, void*);

main.c

ins(F,(void*)&Proc);
ins(F2,(void*)&x);

proc = (Proc*)remover(F);

int comparar_proc(void* p1,void* p2){

}
